# Текущие проблемы

Состояние на 15.08.2026, после первого прогона на судье (`logs/first_try_log`,
**15601.6 / 22000**) и постройки локального набора.

Порядок — по размеру потенциального выигрыша, а не по сложности.

---

## 1. Декод-режим: 30–85 баллов из 1000 (главная дыра)

Тесты `tests/gen/decode_*` (малый `Lin`, большой `Lout` — узкое место сам оборот декода):

| тест | v6 | v7 |
|---|---|---|
| decode_w10_k8 (w_tp=1.0) | 84.4 | 67.8 |
| decode_w05_k8 | 39.2 | 35.0 |
| decode_w05_k2 | 33.8 | 32.3 |
| decode_w00_k8 (w_tp=0) | 0.0 | 0.0 |

У судьи это, скорее всего, тесты **#5 (415) и #6 (382)** — там высокий абсолютный `tp`,
но `norm_tp` всего 0.27–0.31, а веса `w_tp` = 0.8 и 0.9. Только на этих двух тестах
лежит **+1200 баллов**.

**Что известно точно.** `util_E = 0.967` — упирается в локальный сервер. Стоимость
одного токена на E равна

    (S + decode_pre(a))/a + (S + decode_post(b))/b,

где `a` и `b` — размеры групп D PRE и D POST. Замер на `decode_w10_k8`:

| | размер D PRE | размер D POST | размер D PROC | tp |
|---|---|---|---|---|
| v6 | 34.3 | 18.1 | 8.8 | 2.51 |
| v7 | 68.0 | 18.5 | 8.8 | 2.04 |

Видно, что `b` не растёт вслед за `a`. Причина структурная: группа `D PRE` из `a`
запросов размазана по всем K серверам, обратно приходит **K отдельных DOWN-переводов**,
и `D POST` собирается из того, что успело вернуться, то есть `b ≈ a/K`. Именно `b`
доминирует в стоимости: 0.233 из 0.374 мс на токен.

Если бы `b` был равен `a` (34 вместо 18), `tp` вырос бы примерно на **40%** без единого
других изменения.

**Что уже пробовали — и почему не сработало:** см. раздел 5. Все три прямых способа
увеличить `b` провалились. Нужен четвёртый заход, и его стоит искать не в размере групп,
а в том, чтобы E реже платил `S`.

---

## 2. Локальный набор не даёт градиента там, где у судьи вес на ожиданиях

Три теста стабильно дают ровно 0 у обеих версий: `burst_sub_w00`, `decode_w00_k8`,
`slowlink_w00`. Это **дефект генератора**, а не решения.

`harness/model.py` калибрует `SLO2 = f2 × tpot` последовательного эталона. Но у эталона
в полёте ровно один запрос, поэтому его `tpot = 12.63` мс. Любой батчевый планировщик с
пулом в 120 запросов имеет `tpot ≥ 46` мс просто по определению оборота. При `f1=0.4`,
`f2=0.5` получается `dist_base = 1.80`, то есть для ненулевого балла нужен
`tpot < 17.7` мс — недостижимо ни при какой стратегии.

У судьи такого нет: его тесты с `w_tp=0` (#3 и #7) дают 500 и 909 баллов, то есть цели
там достижимые. Значит, судья калибрует SLO не от последовательного эталона.

**Чинить так:** считать `SLO2` от оценки установившегося оборота
(`max(pool × стоимость_на_токен, rtt)` — та же формула, что в решении), а не от эталона.
Пока это не сделано, любые улучшения TPOT локально невидимы, и **вся половина
скоринга с весом `w_c` тюнится вслепую**.

Побочно: `throughput_ceiling()` перебирает размеры групп до 4096, не ограничивая их
числом запросов `R`. На `decode_w10_k8` это даёт `tp_UB = 28.9` при физическом потолке
около 7.5 и реально достижимых 2.6 — то есть `norm_tp` там недостижим по построению.

---

## 3. v7 не лучше v6 в сумме

Локальный набор, 18 тестов: **v7 = 6773.4, v6 = 6776.6**.

Весь выигрыш v7 сосредоточен в одном семействе (`burst_super`: 280 → 375, +94 за счёт
критерия размера группы), потери размазаны по декод-режиму (−16 на `decode_w10_k8`) и
`fast_*` (−9.5, −7.2).

То есть **на судью v7 идёт как ставка, а не как гарантированное улучшение**. Разумные
основания для ставки: корректная обработка `-1` в таблице (v6 интерполирует сквозь
пропуски, что противоречит task.md:130) и поведение на суперлинейных таблицах —
судейский **#11** стоит ровно на уровне последовательного эталона (`norm_tp = 0.0004`),
что как раз похоже на патологическую таблицу.

---

## 4. Что в логе судьи выглядит неулучшаемым

Не тратить на это время:

- **#1** (500.0) — это Example 1 из условия: один запрос, один токен. Параллелизма нет,
  критический путь фиксирован, `norm_tp = 0` неизбежен.
- **#2** (500.0) — `tpot = 0` (у всех запросов `Lout = 1`), малый `tdr`, система
  недогружена: `tp` упирается в темп прибытий, а не в расписание.

Вместе это 1000 из 22000, которые никакая стратегия не поднимет.

---

## 5. Тупики: проверено замером, не повторять

| гипотеза | замер |
|---|---|
| ждать накопления волны перед D PRE / D POST | 6164 → **5799** |
| волна целиком на один сервер (один DOWN → неразрезанный D POST) | 6300 → **5684** |
| концентрация пула на части серверов (ради крупных волн) | 6300 → **3283** |
| разбиение `P PROC` на куски без гейта | 6113 → **5647** |

Почему:

1. **Ожидание.** Волны расслаиваются в конвейер сами, только если запускать готовое
   немедленно. Пока E ждёт недостающих участников, они физически не могут прийти раньше
   полного оборота — ожидание схлопывает конвейер в одну волну и оставляет E простаивать.
2. **Волна с одного сервера.** Даёт неразрезанный `D POST`, но её размер — `pool/K`, и
   потеря на размере перевешивает выигрыш от целостности.
3. **Концентрация.** На тех же серверах лежит `P PROC`, который на `burst_*` и есть
   узкое место (`util_C_max = 0.971`); плюс запросы на «отключённых» серверах голодают.
4. **Разбиение `P PROC`.** Каждый кусок стоит своего `S` на облаке, а префилл
   растягивается и портит TDR. Оставлено под гейтом «на сервере ждёт волна И его
   загрузка < 0.7», при котором на текущем наборе не срабатывает ни разу — то есть
   **фактически не покрыто тестами**.

---

## 6. Мелкое, но незакрытое

- **`superlinear_nogroup` просел**: 312.9 → 261.4. Легаси-тест ровно на тот случай, ради
  которого вводился критерий размера группы, при этом на большом `burst_super` критерий
  даёт +94. Тест крошечный (6 запросов, 12 токенов), там правит латентность, а не
  установившийся режим — но разобрано не было.
- **`tests/*.expected` устарели**: это сводки Rust-версии v2, снятые до перехода на C++.
  `run_all.sh` из-за них красный по diff'у, хотя нарушений нет. Либо переснять
  (`--bless`), либо считать легаси-тесты только проверкой легальности через `bench.py`.
- **Интерполяция между строками таблицы на реальных данных не проверена.** Генератор
  строит гладкие степенные колонки; как выглядят судейские таблицы — неизвестно.
- **Счётчик `inFlight`** (гарантия будущего события, нужен для любых стратегий с
  ожиданием) клампится по нулю снизу, но его точность отдельно не проверялась. Сейчас
  на поведение по умолчанию не влияет, так как ожидания выключены.

---

## Как воспроизвести замеры

```bash
g++ -O2 -std=c++17 -o build/v6 src/2251A_v6.cpp
g++ -O2 -std=c++17 -o build/v7 src/2251A_v7.cpp
python3 harness/gen.py --stress                                  # tests/gen/*
python3 harness/bench.py --bin v6=build/v6 --bin v7=build/v7     # A/B по всем тестам
python3 harness/interactor.py tests/gen/decode_w10_k8.txt -- ./build/v7   # один тест
```

`interactor.py` печатает `util_E / util_C_max / util_UP / util_DOWN` — **с загрузки и
надо начинать разбор любого теста**, она сразу говорит, во что упёрлись. Переключатели
для ablation: `SCHED_POLICY`, `SCHED_WAIT_PRE`, `SCHED_WAIT_POST`, `SCHED_DEBUG=<N>`.

Ограничения соблюдаются с запасом: 0.47 с в пересчёте на 2·10⁶ фреймов (лимит 15 с),
13.5 МБ (лимит 256 МБ), нарушений протокола нет ни на одном тесте.
